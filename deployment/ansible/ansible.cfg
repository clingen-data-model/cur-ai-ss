[defaults]
# Suppress the Python interpreter warning
interpreter_python = auto_silent

[ssh_connection]
# Enable pipelining to avoid temporary file permission issues
# when using become_user with unprivileged users from macOS control machines
pipelining = True
