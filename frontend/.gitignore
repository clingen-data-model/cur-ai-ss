# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
build
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Development
.agents
# Per-machine, installed by `pnpm dlx skills add shadcn/ui`. CLAUDE.md already
# describes these as gitignored; they were not.
.claude/skills
.env.local
.env.*.local
.vscode
.idea
*.swp
*.swo
*~

# Testing
coverage
.nyc_output

# API client and spec (generated, not checked in)
api-spec.json
src/api/generated
